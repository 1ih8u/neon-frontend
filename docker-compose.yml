services:
  frontend:
    build:
      context: .
      args:
        VITE_API_URL: ${VITE_API_URL}
    image: neon-frontend:local
    environment:
      - VITE_API_URL=${VITE_API_URL}
    ports:
      - "8080:80" 